# react-router-hono-server pede @types/react@19 como peer, mas o projeto usa
# @types/react@18. É só type-checking (inofensivo em runtime). legacy-peer-deps
# faz o npm instalar mesmo com esse conflito — necessário p/ build limpo no Render.
legacy-peer-deps=true
